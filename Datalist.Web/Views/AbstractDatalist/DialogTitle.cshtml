@{
    ViewBag.Title = "AbstractDatalist.DialogTitle";
}

<div class="panel panel-default">
    <div class="panel-heading">DialogTitle</div>
    <div class="panel-body">
        <strong>Description</strong>
        <pre>Specifies datalist dialog window title.</pre>
        <strong>Defaults</strong>
        <pre>Implementing type's name with "Datalist" keyword replaced to an empty string.</pre>
        <strong>Remarks</strong>
        <pre>Dialog title will be visible for datalist dialog window.</pre>
        <strong>Examples</strong>
        <pre>
<strong>Implementing type:</strong> DefaultDatalist
<strong>DialogTitle:</strong> <span class="str">"Default"</span>

Datalist dialog title will be <span class="str">"Default"</span>.</pre>
        @Html.Datalist("CurrentFilter1", null, new DefaultDatalist())
        <div class="separator"></div>
        <pre>
<strong>Implementing type:</strong> ExampleDatalist
<strong>DialogTitle:</strong> <span class="str">"Normal dialog title"</span>

Datalist dialog title will be <span class="str">"Normal dialog title"</span>.

<span class="kwrd">public</span> ExampleDatalist()
{
    DialogTitle = <span class="str">"Normal dialog title"</span>;
}</pre>
        @Html.Datalist("CurrentFilter1", null, new ExampleDatalist())
    </div>
</div>
