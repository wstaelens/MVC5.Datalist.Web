@{
    ViewBag.Title = "GenericDatalist.GetColumnCssClass";
}

<div class="panel panel-default">
    <div class="panel-heading">GetColumnCssClass()</div>
    <div class="panel-body">
        <strong>Description</strong>
        <pre>Get's an css class for column.</pre>
        <strong>Remarks</strong>
        <pre>This method is always returns empty string. If overriden, specified class will be applied for column header and all it's data rows.</pre>
        <strong>Examples</strong>
        <pre>
Center aligns all <span class="cls">String</span> properties.

<span class="kwrd">protected</span> <span class="kwrd">override</span> <span class="cls">String</span> GetColumnCssClass(PropertyInfo property)
{
    <span class="kwrd">if</span> (property.PropertyType == <span class="kwrd">typeof</span>(<span class="cls">String</span>))
        <span class="kwrd">return</span> <span class="str">"text-cell"</span>;

    <span class="kwrd">return</span> <span class="cls">String</span>.Empty;
}

<span style="color: #C7254E">.text-cell</span> {
    <span style="color: #FF0000">text-align</span><span style="color: #0000FF">: center</span>;
}</pre>
        @Html.Datalist("GetColumnCssClass1", null, new ExampleDatalist())
    </div>
</div>